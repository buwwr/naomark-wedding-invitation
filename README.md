# Wedding Invitation

Built using React + TailwindCSS + DaisyUI. Also, integrated with Google Form. 
This is designed to be mobile-centric, so when viewed on wider screens, the content remains centered within a fixed width.

